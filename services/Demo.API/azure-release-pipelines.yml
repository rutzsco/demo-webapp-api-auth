trigger: none

resources:
  pipelines:
  - pipeline: build
    source: 'demo-webapp-api-auth - API - BUILD'
  
variables:
  azureSubscription: 'Demo Acount'

stages:
- template: azure-release-pipelines-stage-template.yml
  parameters:
    stageName: 'CI'
    environment: 'CI'
    azureSubscription: ${{ variables.azureSubscription }}
    webAppName: 'demo-webapp-api-auth-ci'
    resourceGroupName: 'demo-webapp-api-auth-api-ci'
    region: 'East US'
    packageName: 'Demo.API.zip'